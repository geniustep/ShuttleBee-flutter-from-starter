# دليل التوطين العربي - Arabic Localization Guide

## نظام الأرقام وتنسيق التاريخ

تم إضافة نظام متكامل للتحكم في نوع الأرقام (عربية/فرنسية) وتنسيقات التاريخ المختلفة.

## المميزات الجديدة

### 1. نظام الأرقام (Numeral System)

#### الأرقام الافتراضية
- عند استخدام اللغة العربية، الأرقام الافتراضية هي **الفرنسية (0-9)**
- يمكن للمستخدم التبديل إلى **الأرقام العربية (٠-٩)** من الإعدادات
- **مهم**: أسماء الشهور والأيام تبقى دائماً باللغة المختارة (عربية/إنجليزية/فرنسية)، فقط الأرقام (0-9) هي التي تتحول

#### كيفية الاستخدام

**في الإعدادات:**
1. اذهب إلى الإعدادات → المظهر
2. اختر "نظام الأرقام"
3. اختر بين:
   - أرقام فرنسية (0-9) - الافتراضي
   - أرقام عربية (٠-٩)

**في الكود:**
```dart
// استخدام Formatters - تطبق الإعدادات تلقائياً
final formatted = Formatters.number(12345); 
// النتيجة: "12,345" أو "١٢٬٣٤٥" حسب الإعدادات

// استخدام NumberConverter مباشرة
final arabic = NumberConverter.toArabicNumerals("123");
// النتيجة: "١٢٣"

final western = NumberConverter.toWesternNumerals("٠١٢٣");
// النتيجة: "0123"
```

### 2. تنسيق التاريخ (Date Format)

#### التنسيقات المتاحة
- **مختصر (Short)**: `01/12/2024`
- **متوسط (Medium)**: `01 Dec 2024` - الافتراضي
- **طويل (Long)**: `01 December 2024`
- **كامل (Full)**: `Sunday, 01 December 2024`

#### كيفية الاستخدام

**في الإعدادات:**
1. اذهب إلى الإعدادات → المظهر
2. اختر "تنسيق التاريخ"
3. اختر التنسيق المفضل مع معاينة مباشرة

**في الكود:**
```dart
// استخدام Formatters - تطبق الإعدادات تلقائياً
final date = DateTime.now();
final formatted = Formatters.displayDate(date);
// النتيجة تعتمد على إعدادات المستخدم

// استخدام تنسيق محدد
final custom = Formatters.date(date, pattern: 'yyyy-MM-dd');
```

## الملفات المضافة/المعدلة

### ملفات جديدة
1. `lib/core/utils/number_converter.dart` - أداة تحويل الأرقام
2. `ARABIC_LOCALIZATION_GUIDE.md` - هذا الملف

### ملفات معدلة
1. `lib/core/constants/storage_keys.dart` - مفاتيح تخزين جديدة
2. `lib/core/utils/formatters.dart` - دعم الأرقام العربية والتنسيقات
3. `lib/shared/providers/global_providers.dart` - providers جديدة
4. `lib/l10n/app_localizations.dart` - ترجمات جديدة
5. `lib/features/settings/presentation/screens/settings_screen.dart` - إعدادات جديدة
6. `lib/bootstrap/app_initializer.dart` - تهيئة الإعدادات

### ملفات محدثة لاستخدام Formatters (تطبق الإعدادات تلقائياً)
7. `lib/features/dispatcher/presentation/screens/dispatcher_trips_screen.dart`
8. `lib/features/dispatcher/presentation/screens/dispatcher_trip_detail_screen.dart`
9. `lib/features/dispatcher/presentation/screens/dispatcher_home_screen.dart`
10. `lib/features/dispatcher/presentation/screens/dispatcher_create_trip_screen.dart`
11. `lib/features/dispatcher/presentation/screens/dispatcher_edit_trip_screen.dart`
12. `lib/features/dispatcher/presentation/widgets/select_trip_for_absence_sheet.dart`

## كيف يعمل النظام - التفاصيل التقنية

### آلية عمل نظام الأرقام والتواريخ

#### 1. الأرقام والشهور منفصلة تماماً
```
عند استخدام العربية:
- أسماء الشهور: "يناير"، "فبراير"، "ديسمبر" (دائماً بالعربية)
- الأرقام: حسب إعداد المستخدم
  * إعداد افتراضي: 0-9 → "01 ديسمبر 2024"
  * بعد التبديل: ٠-٩ → "٠١ ديسمبر ٢٠٢٤"
```

#### 2. عند بدء التطبيق
```dart
// في app_initializer.dart
await _initializeFormatPreferences();
// يتم تحميل:
// 1. اللغة المحفوظة (ar/en/fr) → لتحديد أسماء الشهور
// 2. نوع الأرقام (عربية/فرنسية) → لتحويل الأرقام
// 3. تنسيق التاريخ (مختصر/متوسط/طويل/كامل)
```

#### 3. عند تغيير اللغة
```dart
// في LocaleNotifier
await setLocale(Locale('ar'));
// يتم:
// 1. حفظ اللغة الجديدة
// 2. تحديث Formatters.setLocale('ar') → لتحديث أسماء الشهور
// 3. واجهة التطبيق تتحدث تلقائياً
```

#### 4. عند تغيير نوع الأرقام
```dart
// في ArabicNumeralsNotifier
await setUseArabicNumerals(true);
// يتم:
// 1. حفظ الإعداد
// 2. تحديث Formatters.setNumeralPreference(true)
// 3. جميع الأرقام في التطبيق تتحول فوراً
```

#### 5. عند استخدام التنسيق
```dart
// جميع دوال Formatters تطبق الإعدادات تلقائياً
Formatters.displayDate(DateTime(2024, 12, 1));
// الخطوات:
// 1. استخدام DateFormat مع locale الحالي
//    - إذا ar → "01 ديسمبر 2024"
//    - إذا en → "01 December 2024"
//    - إذا fr → "01 décembre 2024"
// 
// 2. التحقق من شرطين معاً:
//    أ) هل اللغة الحالية عربية؟ (_locale == 'ar')
//    ب) هل المستخدم فعّل الأرقام العربية؟ (_useArabicNumerals == true)
//
// 3. فقط إذا كان الجوابان نعم → تحويل الأرقام إلى عربية
//    "01 ديسمبر 2024" → "٠١ ديسمبر ٢٠٢٤"
//
// 4. إرجاع النتيجة النهائية

// أمثلة أخرى:
Formatters.number(1234);        
// العربية مع أرقام عربية: ١٬٢٣٤
// أي حالة أخرى: 1,234

Formatters.currency(99.99);     
// العربية مع أرقام عربية: $٩٩٫٩٩
// أي حالة أخرى: $99.99

Formatters.phone("0123456789"); 
// العربية مع أرقام عربية: (٠١٢) ٣٤٥-٦٧٨٩
// أي حالة أخرى: (012) 345-6789
```

## أمثلة عملية

### مثال 1: سيناريو كامل لجميع اللغات

```dart
final date = DateTime(2024, 12, 18);

// ============= اللغة العربية =============
Formatters.setLocale('ar');

// حالة 1: أرقام فرنسية (افتراضي)
Formatters.setNumeralPreference(false);
Formatters.displayDate(date);  // "18 ديسمبر 2024"  ✅

// حالة 2: أرقام عربية
Formatters.setNumeralPreference(true);
Formatters.displayDate(date);  // "١٨ ديسمبر ٢٠٢٤"  ✅

// ============= اللغة الإنجليزية =============
Formatters.setLocale('en');

// حالة 3: أرقام فرنسية (دائماً)
Formatters.setNumeralPreference(false);
Formatters.displayDate(date);  // "18 December 2024"  ✅

// حالة 4: حتى لو فعّلنا الأرقام العربية!
Formatters.setNumeralPreference(true);
Formatters.displayDate(date);  // "18 December 2024"  ✅
                               // لاحظ: ما تغيرت! 

// ============= اللغة الفرنسية =============
Formatters.setLocale('fr');

// حالة 5: أرقام فرنسية (دائماً)
Formatters.setNumeralPreference(false);
Formatters.displayDate(date);  // "18 décembre 2024"  ✅

// حالة 6: حتى لو فعّلنا الأرقام العربية!
Formatters.setNumeralPreference(true);
Formatters.displayDate(date);  // "18 décembre 2024"  ✅
                               // لاحظ: ما تغيرت!
```

### مثال 2: عرض تاريخ باللغة العربية
```dart
// عند استخدام اللغة العربية:
final date = DateTime(2024, 12, 18);

// مع الأرقام الفرنسية (افتراضي):
Formatters.displayDate(date)  // "18 ديسمبر 2024"

// مع الأرقام العربية:
Formatters.displayDate(date)  // "١٨ ديسمبر ٢٠٢٤"

// لاحظ: اسم الشهر "ديسمبر" يبقى بالعربية في الحالتين ✅
```

### مثال 3: عرض وقت
```dart
final time = DateTime(2024, 12, 18, 14, 30);

// مع الأرقام الفرنسية:
Formatters.time(time, use24Hour: true)  // "14:30"

// مع الأرقام العربية:
Formatters.time(time, use24Hour: true)  // "١٤:٣٠"
```

### مثال 4: استخدام في Widget
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../core/utils/formatters.dart';
import '../shared/providers/global_providers.dart';

class ExampleWidget extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final useArabicNumerals = ref.watch(arabicNumeralsProvider);
    final dateFormat = ref.watch(dateFormatProvider);
    
    return Column(
      children: [
        // عرض رقم
        Text(Formatters.number(12345)),
        // النتيجة: "12,345" أو "١٢٬٣٤٥"
        
        // عرض تاريخ
        Text(Formatters.displayDate(DateTime.now())),
        // مثلاً: "18 ديسمبر 2024" أو "١٨ ديسمبر ٢٠٢٤"
        
        // عرض مبلغ مالي
        Text(Formatters.currency(99.99, symbol: 'دج')),
        // النتيجة: "دج99.99" أو "دج٩٩٫٩٩"
        
        // زر للتبديل بين الأرقام
        ElevatedButton(
          onPressed: () {
            ref.read(arabicNumeralsProvider.notifier).toggle();
          },
          child: Text(useArabicNumerals 
            ? 'التبديل إلى الأرقام الفرنسية' 
            : 'التبديل إلى الأرقام العربية'),
        ),
      ],
    );
  }
}
```

## ملاحظات مهمة

### 🎯 الفرق بين الأرقام وأسماء الشهور

**النقطة الأساسية**: الأرقام وأسماء الشهور يتحكم بهما إعدادان منفصلان تماماً:

| العنصر | يتحكم به | أمثلة |
|--------|----------|-------|
| **أسماء الشهور والأيام** | **اللغة** (عربي/إنجليزي/فرنسي) | ديسمبر، December، décembre |
| **الأرقام (0-9)** | **نوع الأرقام** (فرنسية/عربية) **فقط للعربية** | 18، ١٨ |

**⚠️ مهم جداً**: 
- خيار الأرقام العربية (٠-٩) **متاح فقط** عند استخدام اللغة العربية
- عند استخدام الإنجليزية أو الفرنسية، الأرقام دائماً 0-9 (لا يمكن تغييرها)

**أمثلة عملية**:

**اللغة العربية:**
```
• الأرقام الفرنسية (افتراضي): "18 ديسمبر 2024"  ✅
• الأرقام العربية:              "١٨ ديسمبر ٢٠٢٤"  ✅
```

**اللغة الإنجليزية:**
```
• الأرقام دائماً فرنسية: "18 December 2024"  ✅
• لا يوجد خيار للأرقام العربية
```

**اللغة الفرنسية:**
```
• الأرقام دائماً فرنسية: "18 décembre 2024"  ✅
• لا يوجد خيار للأرقام العربية
```

### 🔒 القواعد الصارمة للنظام

#### قاعدة 1: الأرقام العربية للغة العربية فقط
```dart
// في Formatters._applyNumeralFormat()
if (_locale == 'ar' && _useArabicNumerals) {
  return NumberConverter.toArabicNumerals(text);
}
return text;
```

**معنى هذا**:
- ✅ لغة عربية + إعداد أرقام عربية = تحويل الأرقام
- ❌ لغة إنجليزية + إعداد أرقام عربية = لا تحويل (تجاهل الإعداد)
- ❌ لغة فرنسية + إعداد أرقام عربية = لا تحويل (تجاهل الإعداد)
- ❌ لغة عربية + إعداد أرقام فرنسية = لا تحويل

#### قاعدة 2: إخفاء الخيار في الإعدادات
```dart
// في SettingsScreen
if (locale.languageCode == 'ar')  // فقط عند العربية
  SettingsTile(
    title: l10n.translate('numeral_system'),
    // ...
  ),
```

**معنى هذا**:
- عند استخدام الإنجليزية أو الفرنسية: الخيار لا يظهر أصلاً في الإعدادات
- عند التبديل من العربية لأي لغة أخرى: الخيار يختفي تلقائياً

### ملاحظات عامة

1. **الأرقام الافتراضية**: عند استخدام اللغة العربية، الأرقام الفرنسية (0-9) هي الافتراضية
2. **الإعداد محلي**: يتم حفظ الإعداد على الجهاز ويبقى بعد إعادة فتح التطبيق
3. **التطبيق التلقائي**: جميع الأرقام والتواريخ في التطبيق تستخدم الإعدادات تلقائياً
4. **الحماية من الأخطاء**: حتى لو كان الإعداد محفوظاً، لن يتم تطبيق الأرقام العربية إلا مع اللغة العربية
5. **الصفحات المحدثة**: تم تحديث جميع صفحات الـ Dispatcher الرئيسية لاستخدام النظام الجديد
6. **الصفحات الأخرى**: لتحديث صفحات أخرى، استبدل استخدامات `DateFormat` بـ `Formatters`

### لتحديث صفحة جديدة:
1. أضف: `import '../../../../core/utils/formatters.dart';`
2. استبدل: `DateFormat('...').format(date)` بـ `Formatters.displayDate(date)`
3. استبدل: `DateFormat('HH:mm').format(time)` بـ `Formatters.time(time, use24Hour: true)`
4. احذف استيراد `intl.dart` إذا لم يعد مستخدماً

## الدعم الفني

إذا كنت بحاجة لتخصيص إضافي:
- راجع ملف `number_converter.dart` لإضافة تحويلات خاصة
- راجع ملف `formatters.dart` لإضافة تنسيقات جديدة
- راجع ملف `global_providers.dart` لفهم إدارة الحالة

---

**تاريخ الإنشاء**: ديسمبر 2024
**الإصدار**: 1.0.0
